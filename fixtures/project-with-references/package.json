{
  "name": "project-with-references",
  "private": true,
  "scripts": {
    "build": "rollup -c",
    "build:a": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "rollup": "^4.27.3",
    "rollup-plugin-dts": "../.."
  }
}
