import { In, Out } from "../settings";

/**
 * @public
 */
export const config = {
  normalize: (inVar: In) => ({ b: 1 }),
};
/**
 * @public
 */
export const options = {
  normalize: (inVar: In) => ({ b: 1 }),
};
/**
 * @public
 */
export const params = {
  normalize: (inVar: In) => ({ b: 1 }),
};
